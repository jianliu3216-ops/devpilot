# DevPilot 升级指南

> 每次 `git pull` 拉取最新代码后，按以下步骤更新本地 Skill。
> 以下命令在 **Git Bash** 中执行（安装 git for Windows 时自带）。

---

## 自动更新（推荐）

拉取最新代码后，在工程根目录执行：

```bash
bash update-skills.sh
```

脚本做了什么：
- **仅删除** `~/.claude/skills/jit-*` 开头的 Skill（6 个），不动你安装的其他 Skill
- **重新安装** 当前工程 `skills/` 下的全部最新 Skill

重启 Claude Code 后生效（`/exit` → `claude`）。

---

## 手动清理旧版 Skill

v1.0.2.0 之前安装的 Skill 不带 `jit-` 前缀，`update-skills.sh` 不会自动删除它们。

**先看看有哪些旧版：**

```bash
# Git Bash
ls -d "$HOME/.claude/skills/"*/ | grep -v "jit-"
```

**确认后删除（二选一）：**

```bash
# 方式1：Git Bash 一条命令
rm -rf "$HOME/.claude/skills/env-auto-setup" \
       "$HOME/.claude/skills/generate-prd" \
       "$HOME/.claude/skills/implement-code" \
       "$HOME/.claude/skills/nowTimeAndModel" \
       "$HOME/.claude/skills/project-devpilot-status" \
       "$HOME/.claude/skills/project-cicd-status" \
       "$HOME/.claude/skills/project-knowledge-base" \
       "$HOME/.claude/skills/requirement-analysis" \
       "$HOME/.claude/skills/requirement-change" \
       "$HOME/.claude/skills/software-design" \
       "$HOME/.claude/skills/test-cases" \
       "$HOME/.claude/skills/test-report" \
       "$HOME/.claude/skills/ui-ux-pro-max" \
       "$HOME/.claude/skills/devpilot-init"
```

```cmd
:: 方式2：Windows CMD（以实际用户目录为准）
rd /s /q "%USERPROFILE%\.claude\skills\env-auto-setup"
rd /s /q "%USERPROFILE%\.claude\skills\generate-prd"
...（依次删除上面列出的 14 个目录）
```

---

## 验证

```bash
# Git Bash
ls "$HOME/.claude/skills/" | grep jit-
```

应看到 6 个 `jit-*` 开头的 Skill。
