# Autopilot 智能流水线开发流程

> **导航**: [快速开始](QUICK_START.md) | [知识库规则](docs/KNOWLEDGE_BASE_RULES.md) | [安装指南](QUICK_START.md#安装)

本项目是一套基于 Claude Code 的 AI 辅助开发智能流水线，实现从简单自然语言需求 → 需求分析 → PRD → 软件设计 → 代码实现 → 测试用例 → 测试报告的全流程自动化。每个阶段产出需要人工确认后才进入下一阶段，确保质量可控。

---

## 设计思路

当前目录 `本仓库根目录（claude-code-autopilot）` 是**共用框架目录**，只存放：
- 所有 Agent 定义（`.claude/agents/`）
- 所有钩子脚本（`.claude/hooks/`）
- 框架核心代码（`.claude-collective/`）
- 全局行为规则（`CLAUDE.md`）
- 使用说明（`README.md` 本文件）

**当你要分析某个项目 / 开发某个功能时**：
- 你提供目标项目目录路径（比如 `D:\projects\my-existing-project`）
- 所有生成的产出文档（私域知识库、需求分析、PRD、设计、测试用例、测试报告）都**输出到目标项目目录下**
- 不会污染当前共用框架目录，避免文件冲突
- 每个项目自己保管自己的文档，清晰分离

---

## 目录结构

框架目录关键文件：

| 目录/文件 | 作用 |
|-----------|------|
| `.claude/agents/` | Agent 定义（30+ 个专业 Agent） |
| `.claude/hooks/` | 钩子脚本（SessionStart 注入规则等） |
| `.claude/settings.json` | 项目级设置 |
| `.claude-collective/` | 框架核心（cicd-rules.md 是规则摘要） |
| `skills/` | Autopilot Skill 定义（13 个，含 UI/UX 设计增强） |
| `CLAUDE.md` | 项目行为规则 |
| `README.md` | 本文件（AI 规则书） |
| `QUICK_START.md` | 快速参考 |
| `install.sh` | 安装脚本 |

> 完整目录结构详见 [docs/DIRECTORY_STRUCTURE.md](docs/DIRECTORY_STRUCTURE.md)

### 目标项目产出目录（按需求隔离）

```
你的目标项目/
├── docs/
│   ├── knowledge-base/                  → 项目知识库（共享，增量更新）
│   ├── user-login/                      → 需求A（一个需求一个目录）
│   │   ├── 01-requirements-analysis.md
│   │   ├── 02-prd.md
│   │   ├── 03-software-design.md
│   │   ├── 04-test-cases.md
│   │   ├── 05-test-report.md
│   │   └── CHANGELOG.md
│   └── ...
├── tests/{需求标识}/                     → 测试代码（按需求隔离）
└── (项目源代码...)
```

### 测试代码目录规则

- **位置**：`[目标项目]/tests/{需求标识}/`，与 `docs/` 同级
- **不放在源码子目录里**：避免测试代码散落在深层业务目录中，保持项目根目录结构清晰
- **按需求隔离**：每个需求的测试代码独立目录，互不干扰
- **目录确认机制**：生成测试代码前，AI 必须先输出目录路径供用户确认，支持自定义调整

**目录确认流程（任务6/任务8 生成测试代码时）：**
```
AI: 📂 测试代码目录确认
    默认路径：D:\projects\my-app\tests\user-login\
    包含文件：
      - run_tests.js          （测试运行脚本）
      - test_user_login.lua   （功能测试脚本）

    是否需要调整路径？（直接回车确认，或输入自定义路径）

用户: 直接回车 → 用默认路径
用户: 改到 D:\my-tests\user-login\ → 用自定义路径
```

### 需求标识规则

每个需求有一个**英文短标识**（kebab-case），用于创建独立的文档目录。用户只需输入中文需求描述，AI 自动建议英文标识，**必须提示用户确认后才能继续**：

```
用户: 需求分析：用户登录注册模块，目标项目：D:\projects\my-app

AI: 需求【用户登录注册模块】英文标识建议：user-login
    是否需要调整？（确认后将以 docs/user-login/ 作为文档目录）

用户: 没问题 → 用 user-login
用户: 改成 login-register → 用 login-register
```

> 关键点：
> - 框架目录和项目产出**完全分离**，不会互相干扰
> - 不同需求之间**完全隔离**，互不覆盖
> - knowledge-base 保持共享（整个项目一份），变更时增量更新
> - 测试代码放在 `tests/{需求标识}/`（与 docs 同级），不放在源码子目录里
> - 生成测试代码前**必须确认目录路径**，防止放错位置

---

## 核心概念：/van 命令是什么？

`/van` 是 claude-code-collective 提供的**自动路由命令**。

**工作原理：**
```
你输入: /van "基于需求分析文档生成 PRD"
          ↓
task-orchestrator (路由 Agent) 分析你的请求
          ↓
自动识别需求 → 派给对应的专业 Agent
          ↓
专业 Agent 开始干活，输出结果
```

**你不需要记**哪个功能应该找哪个 Agent，只需要用 `/van "描述你要做什么"` 就可以了，路由自动完成。

---

## 各阶段任务说明（含 /van 调用示例）

### 任务 1：环境初始化 ✅ 已完成

- **工作内容**：安装 `claude-code-collective` 社区框架，创建目录结构，配置工作规则
- **产出物**：`.claude/agents/`、`CLAUDE.md`、各级文档目录
- **状态**：已完成

---

### 任务 2：生成历史项目私域知识库 ⏳ 等待开始

- **作用**：当您已有历史项目需要接入 AI 辅助开发时，先生成项目私域知识库，让 AI 理解现有代码结构、编码风格、可复用资产。**所有后续阶段都会自动带上这个知识库作为参考**，保证新增代码的风格和结构与现有项目保持一致。**关键：后续 Agent 只需要参考这个知识库理解项目，不需要重新扫描读取整个项目代码**，节省上下文。
- **你的输入示例**：
  ```
  生成知识库，目标项目：D:\projects\my-existing-project
  ```
- **工具**：直接调用您自定义的 Skill → `/project-knowledge-base`
- **实际调用示例（框架自动执行，你不需要输入）**：
  
  ```
  /project-knowledge-base --target "D:\projects\my-existing-project" --output "D:\projects\my-existing-project\docs\knowledge-base\PROJECT_KNOWLEDGE_BASE.md"
  ```
- **输出**：`[目标项目]/docs/knowledge-base/PROJECT_KNOWLEDGE_BASE.md`
  
  - 👉 **输出到目标项目目录，不是输出到框架目录**
  - 如果项目相对复杂进行深度分析 输出`[目标项目]/docs/knowledge-base/PROJECT_KNOWLEDGE_DETAIL.md`
  - 内容：项目架构概览、模块字典说明、可复用工具/组件索引、重构建议
- **你的工作**：只需要告诉我目标项目路径
- **我的工作**：
  
  - 自动在目标项目创建 `docs/knowledge-base/` 目录（如果不存在）
  - 自动调用 skill 生成文档
  - 记住这个文件路径，**所有后续阶段调用 `/van` 时，都会在命令中拼上这个文件的完整绝对路径，并且明确告诉 Agent：请参考这个知识库理解项目，不需要读取整个项目代码**
  - 生成完成后**明确告诉你**：生成了哪个文件，完整路径是什么
- **完成条件**：知识库生成 → **您确认** → 进入下一任务（阶段 3：需求分析）

---

### 任务 3：需求分析 ⏳ 等待开始

- **作用**：把您几句简单的自然语言需求，整理成结构化的需求分析文档，同时主动列出所有待澄清的模糊点。**同时 AI 会自动评估变更级别**，决定后续阶段走标准流程还是轻量流程。
- **依赖**：

  - 如果已完成任务2 → **自动带上** `PROJECT_KNOWLEDGE_BASE.md` 如果存在`PROJECT_KNOWLEDGE_DETAIL`也需要的带上（私域知识库）作为参考
  - 如果跳过任务2（全新项目） → 只依赖您提供的原始需求
- **输入**：目标项目目录路径 + 您提供的原始需求（几句话即可）
- **你的输入示例**：

  ```
  需求分析：D:\projects\my-app，需求：我要做一个用户登录注册模块，支持手机号验证码登录，支持密码登录，记住密码功能。
  ```
  ```
  需求分析：D:\projects\my-app，需求：Spring Boot 从 2.7 升级到 3.2
  ```
- **工具**：通过 `/van` 自动路由到 `prd-research-agent`
- **输出**：`[目标项目]/docs/{需求标识}/01-requirements-analysis.md`
  - 👉 **输出到目标项目的需求目录下，不是输出到框架目录**
  - 内容：项目背景、建设目标、功能需求列表、非功能需求、**待澄清疑问项**（AI 主动列出模糊点请您确认）
- **🔴 变更级别评估（关键环节）**：
  - AI 生成需求分析后，**自动评估变更级别**并输出：
    ```
    📊 变更级别评估

    需求类型：功能新增 / 框架升级 / 批量重构 / ...
    预计影响文件数：约 XX 个
    预计影响模块：XXX
    变更级别：S / M / L

    判定依据：...
    后续流程：标准流程（6阶段）/ 轻量流程（3文档）

    是否调整？（直接回车确认，或输入 S/M/L）
    ```
  - **用户确认级别后，后续所有阶段自动适配**（见"变更分级机制"章节）
  - 用户也可以在输入时直接指定：`级别：L`
- **你的工作**：只需要提供目标路径 + 需求描述（中文即可，AI 会自动建议英文标识并请你确认）
- **我的工作**：
  - 记住目标项目路径
  - **自动生成英文标识建议，提示你确认**（如：需求【用户登录注册模块】英文标识建议：user-login，是否需要调整？）
  - 自动在目标项目创建 `docs/{需求标识}/` 目录（如果不存在）
  - **调用 `/van` 时，会在命令中**拼上所有依赖文件的完整绝对路径** → AI 能直接找到文件，即使新开会话也没问题
  - 如果已完成任务 2（私域知识库），完整调用示例：
    ```
    /van "基于用户提供的原始需求，结合项目私域知识库 E:\项目\你的项目\docs\knowledge-base\PROJECT_KNOWLEDGE_BASE.md，请参考这个知识库理解项目，不需要读取整个项目代码，生成详细的需求分析文档 E:\项目\你的项目\docs\user-login\01-requirements-analysis.md。文档需要包含：1) 项目背景 2) 目标 3) 功能需求列表 4) 非功能需求 5) 待澄清疑问项"
    ```
  - 生成完成后**明确告诉你**：生成了哪个文件，完整路径是什么，变更级别是什么
- **完成条件**：需求分析文档输出 + 变更级别确认 → **您确认并澄清所有疑问** → 按对应级别进入下一任务

---

### 任务 4：生成 PRD 产品需求文档 ⏳ 等待开始

- **作用**：基于澄清后的需求分析，生成完整的 PRD
- **依赖**：
  - 任务2（如果做了）→ 私域知识库 `PROJECT_KNOWLEDGE_BASE.md`如果存在`PROJECT_KNOWLEDGE_DETAIL`也需要的带上
  - 任务3 → 需求分析 `01-requirements-analysis.md`
  - 调用 `/van` 时**自动带上所有依赖**
- **输入**：需求分析已经确认，我已记住目标项目路径，你不需要重复提供，只需要说"生成PRD"
- **你的输入示例**：
  ```
  生成PRD
  ```
- **输出**：`[目标项目]/docs/{需求标识}/02-prd.md`
  - 👉 **输出到目标项目的需求目录下，不是输出到框架目录**
  - 内容：用户故事（谁，需要什么，为什么）、功能规格说明书、交互流程、验收标准
- **我的工作**：
  - 自动拼接完整路径，调用 `/van` 时**自动带上所有依赖文件路径**
  - 如果已完成任务 2（私域知识库），会在调用命令中自动添加：`请参考 PROJECT_KNOWLEDGE_BASE.md 理解项目，不需要读取整个项目代码`
  - 完整调用命令会自动拼好所有依赖的**完整绝对路径**，新开会话也能找到文件
  - 生成完成后**明确告诉你**：生成了哪个文件，完整路径是什么
- **完成条件**：PRD 输出 → **您评审确认** → 进入下一任务（阶段 5：生成软件设计）

---

### 任务 5：生成软件架构设计文档 ⏳ 等待开始

- **作用**：基于 PRD 进行技术设计
- **依赖**：
  - 任务2（如果做了）→ 私域知识库 `PROJECT_KNOWLEDGE_BASE.md`如果存在`PROJECT_KNOWLEDGE_DETAIL`也需要的带上
  - 任务4 → PRD `02-prd.md`
  - 调用 `/van` 时，会在命令中**拼上所有依赖文件的完整绝对路径**，AI 能直接找到文件
- **输入**：PRD 已经确认，我已记住目标项目路径，你不需要重复提供，只需要说"确认PRD，继续软件设计"
- **你的输入示例**：
  ```
  确认PRD，继续软件设计
  ```
- **输出（按变更级别自动适配）**：

  | 变更级别 | 输出文档 | 内容 |
  |---------|---------|------|
  | S/M 级 | `03-software-design.md` | 整体架构图、模块划分、接口定义、数据结构设计、依赖关系 |
  | L 级 | `03-change-strategy.md` | 变更范围、执行策略、分批次计划、风险点、回滚方案 |

  - 👉 **输出到目标项目的需求目录下，不是输出到框架目录**
  - **S/M 级**走标准设计文档
  - **L 级**走轻量策略文档（不需要逐模块详细设计，聚焦"怎么改、分几批、怎么回滚"）
- **L 级策略文档示例**：
  ```markdown
  # 变更策略文档

  ## 变更概述
  - 变更类型：框架升级
  - 影响范围：约 80 个文件，跨所有模块
  - 风险等级：高

  ## 执行策略
  ### 批次 1：依赖配置更新
  - 修改文件：pom.xml
  - 操作：版本号替换
  - 验证：编译通过

  ### 批次 2：废弃 API 替换
  - 修改文件：import 语句、方法调用
  - 操作：搜索替换 + 人工确认
  - 验证：编译通过 + 单元测试

  ## 回滚方案
  - Git 分支策略：从 feature 分支发起
  - 回滚方式：git revert 整个 MR

  ## 风险点
  | 风险 | 影响 | 缓解措施 |
  |------|------|----------|
  | 废弃 API 未完全替换 | 编译失败 | 全局搜索 + 编译验证 |
  ```
- **我的工作**：
  - 根据任务3确认的变更级别，自动选择输出标准设计文档 or 策略文档
  - 如果变更级别在任务3未确定，此阶段会重新评估并提示确认
  - 文档输出到需求目录（已在任务3创建）
  - 自动拼接完整路径，调用 `/van` 时**自动带上所有依赖文件路径**
  - 如果已完成任务 2（私域知识库），会在调用命令中自动添加：`请参考 PROJECT_KNOWLEDGE_BASE.md 理解项目，不需要读取整个项目代码`
  - 生成完成后**明确告诉你**：生成了哪个文件，完整路径是什么
- **完成条件**：设计/策略文档输出 → **您评审确认** → 进入下一任务（阶段 6：代码实现）

---

### 任务 6：代码实现 ⏳ 等待开始

- **作用**：按照设计文档实现功能代码，遵循 TDD 原则。**特别重要：会自动参考私域知识库，保证编码风格和目录结构和现有项目一致**，代码会写到目标项目正确的位置。
- **关于 TDD 顺序**：TDD "先写测试" 是在这里完成的 → 任务6代码实现阶段已经遵循 TDD：先写测试代码 → 再写产品代码 → 重构。任务7生成测试用例文档只是把已写好的整理成可读文档，不影响 TDD 顺序。
- **依赖**：
  - 任务2（如果做了）→ 私域知识库 `PROJECT_KNOWLEDGE_BASE.md`如果存在`PROJECT_KNOWLEDGE_DETAIL`也需要的带上，指导编码风格和目录结构
  - 任务5 → 软件设计 `03-software-design.md` 或 变更策略 `03-change-strategy.md`
  - 调用 `/van` 时，会在命令中**拼上所有依赖文件的完整绝对路径**，AI 能直接找到
- **输入**：软件设计已经确认，我已记住目标项目路径，你不需要重复提供，只需要说"确认设计，开始代码实现"
- **你的输入示例**：
  ```
  确认设计，开始代码实现
  ```
- **流程（按变更级别自动适配）**：

  | 变更级别 | 实现方式 | 说明 |
  |---------|---------|------|
  | S 级 | 直接改代码 + 自动化测试 | 小改动，无需分批 |
  | M 级 | TDD → 先写测试 → 再写实现 → 重构 | 标准流程 |
  | L 级 | **按策略文档分批次执行**，每批次验证 | 大批量变更不适合逐个 TDD，按批次推进更高效 |

- **L 级批量执行模式**：
  - 按变更策略文档中的批次计划依次执行
  - 每个批次完成后：编译验证 → 自动化测试 → 输出批次结果
  - 全部批次完成后：整体回归验证
  - AI 在执行过程中发现策略未覆盖的问题，会停下来请你确认
- **输出**：**直接输出到目标项目目录**的源代码 + 对应测试代码

  - 👉 业务代码直接写到目标项目源码目录，参考私域知识库放到正确位置
  - 👉 测试代码输出到 `[目标项目]/tests/{需求标识}/`，**生成前必须确认目录路径**
- **测试代码目录确认机制**：
  - AI 生成测试代码前，必须先输出目录路径供用户确认：
    ```
    📂 测试代码目录确认
    默认路径：D:\projects\my-app\tests\user-login\
    包含文件：
      - run_tests.js          （测试运行脚本）
      - test_user_login.lua   （功能测试脚本）

    是否需要调整路径？（直接回车确认，或输入自定义路径）
    ```
  - 用户确认后才开始生成，防止测试代码放错位置
- **我的工作**：
  - 调用 `/van` 时，会在命令中**拼上所有依赖文件的完整绝对路径**，AI 能直接找到
  - 如果已完成任务 2（私域知识库），会在调用命令中自动添加：`请参考 PROJECT_KNOWLEDGE_BASE.md 理解项目，不需要读取整个项目代码`，保证编码风格和目录结构与现有项目一致
  - 完整调用命令会自动拼好所有依赖的**完整绝对路径**，新开会话也能找到文件
  - 运行测试确保通过
  - 完成后**明确告诉你**：代码已生成在目标项目哪个位置，测试结果如何
- **完成条件**：所有功能实现，全部测试通过 → **您评审确认** → 进入下一任务（阶段 7：生成测试用例）

---

### 任务 7：生成测试用例文档 ⏳ 等待开始

- **作用**：整理输出完整的测试用例文档，方便人工评审和回归
- **依赖**：

  - 任务2（如果做了）→ 私域知识库 `PROJECT_KNOWLEDGE_BASE.md` 如果存在`PROJECT_KNOWLEDGE_DETAIL`也需要的带上
  - 任务4 → PRD `02-prd.md`
  - 任务6 → 已实现代码
  - 调用 `/van` 时，会在命令中**拼上所有依赖文件的完整绝对路径**，AI 能直接找到
- **输入**：代码实现已经确认，我已记住目标项目路径，你不需要重复提供，只需要说"确认代码，生成测试用例"
- **你的输入示例**：
  ```
  确认代码，生成测试用例
  ```
- **输出（按变更级别自动适配）**：

  | 变更级别 | 输出文档 | 内容 |
  |---------|---------|------|
  | S 级 | 跳过（自动化测试已覆盖） | 不单独出文档 |
  | M 级 | `04-test-cases.md` | 功能测试用例（正常路径）、边界测试用例、异常测试用例 |
  | L 级 | `04-regression-checklist.md` | 回归验证清单（编译验证、核心功能验证、兼容性验证等勾选项） |

  - 👉 **输出到目标项目的需求目录下，不是输出到框架目录**

- **L 级回归验证清单示例**：
  ```markdown
  # 回归验证清单

  ## 编译验证
  - [ ] 项目编译通过（无 error）
  - [ ] 无新增 deprecated warning

  ## 核心功能验证
  - [ ] 服务启动成功
  - [ ] 健康检查接口正常
  - [ ] 核心业务流程可用

  ## 兼容性验证
  - [ ] API 接口向后兼容
  - [ ] 数据库 schema 兼容
  - [ ] 配置项生效

  ## 自动化测试
  - [ ] 单元测试全部通过
  - [ ] 集成测试全部通过
  ```
- **我的工作**：
  - 文档输出到需求目录（已在任务3创建）
  - 调用 `/van` 时，会在命令中**拼上所有依赖文件的完整绝对路径**，AI 能直接找到文件
  - 如果已完成任务 2（私域知识库），会在调用命令中自动添加：`请参考 PROJECT_KNOWLEDGE_BASE.md 理解项目，不需要读取整个项目代码`
  - 完整调用命令会自动拼好所有依赖的**完整绝对路径**，新开会话也能找到文件
  - 生成完成后**明确告诉你**：生成了哪个文件，完整路径是什么
- **完成条件**：测试用例输出 → **您评审确认** → 进入下一任务（阶段 8：生成测试报告）

---

### 任务 8：生成测试报告 ⏳ 等待开始

- **作用**：执行测试，生成测试报告
- **依赖**：
  - 任务7 → 测试用例 `04-test-cases.md`
  - 任务6 → 已实现代码
  - 调用 `/van` 时**自动带上所有依赖**
- **输入**：测试用例已经确认，我已记住目标项目路径，你不需要重复提供，只需要说"确认用例，生成测试报告"
- **你的输入示例**：
  ```
  确认用例，生成测试报告
  ```
- **输出**：`[目标项目]/docs/{需求标识}/05-test-report.md`
  - 👉 **输出到目标项目的需求目录下，不是输出到框架目录**
  - 测试报告中的测试代码路径引用 `[目标项目]/tests/{需求标识}/`
  - 内容：测试概览（总共多少用例）、测试结果统计（通过/失败数量）、失败用例分析、覆盖率统计
- **我的工作**：
  - 调用 `/van` 时，会在命令中**拼上所有依赖文件的完整绝对路径**，AI 能直接找到文件
  - 生成完成后**明确告诉你**：生成了哪个文件，完整路径是什么，测试结果统计如何
- **完成条件**：测试报告输出 → 自动进入知识库增量更新

---

### 任务 8.5：知识库增量更新（流程闭环）⏳ 等待开始

- **作用**：流程走到最后一步（测试报告完成）后，**必须**执行知识库增量更新，保证知识库始终和代码同步。**不管哪种场景**（正常需求实现或需求变更），只要走到了测试报告完成，就必须更新知识库。
- **核心原则**：
  - **增量更新，不全量重扫**：只更新本次变更涉及的章节，未变更的章节保持不动
  - **AI 已知变更范围**：AI 在整个流程中已经知道改了什么，直接据此更新，不需要重新扫描整个项目
  - **首次生成（任务2）是全量扫描，后续所有更新都是增量**
- **依赖**：
  - 任务2 → 知识库 `PROJECT_KNOWLEDGE_BASE.md`（必须已存在，这是更新不是新建）
  - 本次流程中所有变更的代码和文档
- **输出**：更新 `[目标项目]/docs/knowledge-base/PROJECT_KNOWLEDGE_BASE.md`
  - 👉 只更新受影响的章节，追加更新记录
  - 知识库文档末尾必须包含更新记录表：
    ```markdown
    ## 更新记录

    | 版本 | 日期 | 变更范围 | 说明 |
    |------|------|----------|------|
    | v1.0 | 2026-04-20 | 全量创建 | 初始知识库 |
    | v1.1 | 2026-04-30 | 登录模块 | 新增忘记密码功能，更新模块字典 |
    ```
- **我的工作**：
  - 回顾本次流程中变更了哪些模块/文件
  - 只读取变更涉及的代码，更新知识库对应章节
  - 追加更新记录
  - 生成完成后**明确告诉你**：更新了知识库哪些章节，版本号
- **完成条件**：知识库更新完成 → **您确认** → 本批次开发流程正式结束

---

### 任务 9：需求变更 ⏳ 等待开始

- **作用**：当已有项目需要变更时（修改需求、新增功能、调整设计等），不走全量流程，而是 AI 自动分析变更影响范围，只增量重做受影响的阶段，保留未受影响的产出。
- **核心原则**：
  - **用户只需描述变更内容**，不需要判断"小中大"
  - **AI 自动分析影响范围并分级**：对比现有需求文档、PRD、设计文档，评估变更影响了哪些模块和阶段，自动判断变更级别
  - **增量重做**：只重做受影响的阶段，未受影响的保持不动
  - **最后同样要执行知识库增量更新**（任务8.5），保证流程闭环
- **依赖**：
  - 任务2 → 知识库 `PROJECT_KNOWLEDGE_BASE.md`（必须已存在）
  - 所有已有产出文档（需求分析、PRD、设计、测试用例、测试报告）
- **你的输入示例**：
  ```
  需求变更：user-login 登录页面增加忘记密码链接，点击后跳转到忘记密码页面
  ```
  ```
  需求变更：message-push 新增邮件通知支持
  ```
  ```
  需求变更：spring-boot-upgrade Spring Boot 从 2.7 升级到 3.2
  ```
- **流程步骤**：
  1. **9.1 描述变更**：用户提供变更描述
  2. **9.2 变更影响分析 + 自动分级**：AI 自动分析，输出：
     - 变更影响了哪些模块/文件
     - **自动判定变更级别**（S/M/L），并说明判定依据
     - 推荐的流程路径（标准流程 or 轻量流程）
     - 可以保留的阶段（如：知识库、未受影响的需求项）
  3. **9.3 确认变更范围和级别**：用户确认 AI 的影响分析和分级是否正确，可以调整
  4. **9.4 按对应级别流程执行**（见下方变更分级机制）
  5. **知识库增量更新**：更新知识库
  6. **您确认** → 变更流程结束
- **输出**：更新的文档和代码（都在目标项目目录）
- **我的工作**：
  - 自动对比现有文档，分析变更影响
  - 自动判定变更级别，输出分级建议
  - 输出影响分析报告让您确认
  - 按对应级别的流程执行
  - 最终执行知识库增量更新
- **完成条件**：所有受影响阶段重做完成 + 测试通过 + 知识库更新 → **您确认** → 变更流程结束

---

## 变更分级机制（全流程通用）

### 为什么需要分级？

不管是**新需求**还是**需求变更**，都存在变更规模差异：
- **小变更**：新增一个字段、改一个常量 → 走完整 6 阶段流程太重
- **中变更**：新增功能模块 → 标准流程正好
- **大变更**：框架升级涉及上百文件 → 逐文件写详细设计不现实，需要轻量流程

**分级不是任务9专属，而是全流程通用的自动适配机制**。从任务3（需求分析）开始评估，后续每个阶段自动适配。

### 三级变更定义

| 级别 | 名称 | 判定条件 | 典型场景 |
|------|------|----------|----------|
| **S** | 小变更 | 修改 ≤ 3 个文件，逻辑简单 | 新增字段、修改常量、调整日志格式 |
| **M** | 中变更 | 修改 4-15 个文件，或涉及 2-3 个模块 | 新增功能模块、调整判定逻辑、接口变更 |
| **L** | 大变更 | 修改 > 15 个文件，或跨模块/全局性变更 | 框架版本升级、批量依赖更新、全局重构 |

### 分级触发时机

| 触发点 | 场景 | 说明 |
|-------|------|------|
| **任务3（需求分析）** | 新需求 | AI 分析需求后自动评估级别，**首次分级，后续阶段继承** |
| **任务5（软件设计）** | 设计阶段发现影响范围扩大 | 如果设计时发现比预期更复杂，可升级级别 |
| **任务6（代码实现）** | 实现时发现超出预估 | 如果实现时发现文件数远超预期，可升级级别 |
| **任务9（需求变更）** | 变更需求 | AI 分析变更影响后自动评估级别 |

> **级别只能升级，不能降级**：如果任务3评估为 M 级，任务5发现实际是 L 级，会提示你确认升级。但不会从 M 降为 S。

### AI 自动分级规则

**核心规则：用户不指定级别时，AI 必须自动评估并停下来等用户确认，不能跳过分级直接进入后续阶段。**

AI 在首次分级点（任务3 或任务9.2）自动分析并输出分级结论：

```
📊 变更级别评估

变更描述：Spring Boot 从 2.7 升级到 3.2
预计影响文件数：约 80+ 文件（pom.xml、application.yml、import 语句、废弃 API 替换）
预计影响模块：全局（跨所有模块）
变更级别：L（大变更）

判定依据：
- 影响文件数 > 15 → L 级
- 涉及全局依赖变更 → L 级
- 无新增业务功能，纯技术升级

后续流程：轻量流程（L 级）
是否调整？（直接回车确认，或输入 S/M 修改级别）
```

**两种方式二选一：**

| 方式 | 说明 | 示例 |
|------|------|------|
| **不指定级别（默认）** | AI 自动评估 → 输出分级建议 → **必须等用户确认** → 再进入后续阶段 | `需求分析：升级Spring Boot，目标项目：...` |
| **手动指定级别** | 用户直接指定，跳过自动评估，立即进入对应流程 | `需求分析：升级Spring Boot` |

> ⚠️ **强制规则**：即使用户不指定级别，AI 也不能跳过分级提示直接按某个级别执行。必须先输出分级评估，等用户确认（或调整）后，才能进入下一阶段。

### 三级流程对照

| 阶段 | S 级（小变更） | M 级（中变更） | L 级（大变更） |
|------|:---:|:---:|:---:|
| 需求分析(任务3) | ✅ 简要需求分析 | ✅ 完整需求分析 | ✅ 完整需求分析 |
| PRD(任务4) | 不需要 | ✅ 完整 PRD | ✅ 完整 PRD |
| 软件/策略设计(任务5) | 不需要 | ✅ `03-software-design.md` | ⬇️ `03-change-strategy.md` |
| 代码实现(任务6) | ✅ 直接改代码 | ✅ TDD 流程 | ✅ 按策略分批执行 |
| 测试用例(任务7) | 跳过（自动化覆盖） | ✅ `04-test-cases.md` | ⬇️ `04-regression-checklist.md` |
| 测试报告(任务8) | ✅ 简要结果报告 | ✅ 完整测试报告 | ✅ 完整测试报告 |
| 知识库更新(任务8.5) | ✅ 增量更新 | ✅ 增量更新 | ✅ 增量更新 |

### L 级（大变更）轻量流程详解

L 级变更多数是**技术性批量操作**（非新业务功能），不需要逐模块写详细设计，核心是**策略清晰 + 自动化验证**。

**产出文档（简化为 3 份）：**

| 文档 | 位置 | 内容 |
|------|------|------|
| 变更策略文档 | `docs/{需求标识}/03-change-strategy.md` | 变更范围、执行策略、分批次计划、风险点、回滚方案 |
| 回归验证清单 | `docs/{需求标识}/04-regression-checklist.md` | 验证项清单（编译通过、核心功能可用、API 兼容、配置生效等） |
| 测试报告 | `docs/{需求标识}/05-test-report.md` | 同标准流程 |

**变更策略文档模板：**
```markdown
# 变更策略文档

## 变更概述
- 变更类型：框架升级 / 依赖更新 / 全局重构
- 变更范围：影响 XX 个文件，XX 个模块
- 风险等级：高/中/低

## 执行策略
### 批次 1：依赖配置更新
- 修改文件：pom.xml / build.gradle / package.json
- 操作：版本号替换
- 验证：编译通过

### 批次 2：废弃 API 替换
- 修改文件：import 语句、方法调用
- 操作：搜索替换 + 人工确认
- 验证：编译通过 + 单元测试

### 批次 3：配置迁移
- 修改文件：application.yml / properties
- 操作：配置项迁移
- 验证：启动成功

## 回滚方案
- Git 分支策略：从 feature 分支发起
- 回滚方式：git revert 整个 MR
- 数据兼容：是否有数据迁移，回滚是否需要数据回退

## 风险点
| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| 废弃 API 未完全替换 | 编译失败 | 全局搜索 + 编译验证 |
| 配置项格式变化 | 启动失败 | 逐项对比 + 启动验证 |
```

**回归验证清单模板：**
```markdown
# 回归验证清单

## 编译验证
- [ ] 项目编译通过（无 error）
- [ ] 无新增 deprecated warning

## 核心功能验证
- [ ] 服务启动成功
- [ ] 健康检查接口正常
- [ ] 核心业务流程可用（列出关键路径）

## 兼容性验证
- [ ] API 接口向后兼容
- [ ] 数据库 schema 兼容
- [ ] 配置项生效

## 自动化测试
- [ ] 单元测试全部通过
- [ ] 集成测试全部通过
```

### 用户也可手动指定级别

如果用户明确知道变更大小，可以跳过 AI 自动分级，直接指定：

```
需求变更：spring-boot-upgrade Spring Boot 从 2.7 升级到 3.2，级别：L
```

AI 收到 `级别：L` 后直接走轻量流程，不再做分级分析。

---

## 使用流程（详细步骤）

### 前置条件：如何正确启动 Claude Code 会话

> ❓ **常见问题：如果我随便开一个 Claude Code 会话就可以使用吗？**
> 
> ⚠️ **答：不行。必须 `cd` 到 autopilot 目录启动 Claude Code。**
> 
> Skills 通过 install.sh 注册到 `~/.claude/skills/` 后全局可用，但**流程规则和自动触发**只在 autopilot 项目目录下生效。
> 
> ```bash
> cd 本仓库根目录（claude-code-autopilot）
> claude
> ```

---

### 第一步：选择起点

根据你的情况选择起点：

| 你的情况 | 起点 | 你应该怎么输入 |
|----------|------|----------------|
| 已有历史项目，要让 AI 先理解它 | 从「任务 2：生成历史项目私域知识库」开始 | `生成知识库，目标项目：D:\projects\your-project` |
| 全新项目，从零开始开发新功能 **不需要历史分析** | 从「任务 3：需求分析」开始 | `需求分析：D:\projects\your-new-project，需求：我要做一个用户登录注册模块`（AI 会自动建议英文标识） |

**直接跳过任务 2 完全可以**，设计上就支持。上面给了输入格式示例，照着写就行。

---

### 流程依赖关系

整个流程是**线性依赖**，每个后续阶段都会自动带上前面所有阶段的产出作为输入：

```
任务2：生成私域知识库 PROJECT_KNOWLEDGE_BASE.md（共享，整个项目一份）
          ↓ （所有后续阶段都参考这个）
任务3：生成需求分析 docs/{需求标识}/01-requirements-analysis.md
          ↓ （🔴 首次分级评估：S/M/L，后续阶段自动适配）
任务4：生成 PRD docs/{需求标识}/02-prd.md
          ↓ （设计 需要参考）
任务5：生成软件设计 docs/{需求标识}/03-software-design.md
          ↓ （S/M级）或 变更策略 docs/{需求标识}/03-change-strategy.md ↓ （L级）
任务6：代码实现
          ↓ （S级直接改 / M级TDD / L级按策略分批执行）
任务7：测试用例 docs/{需求标识}/04-test-cases.md（M级）
          ↓ 或 回归验证清单 docs/{需求标识}/04-regression-checklist.md（L级）
          ↓ （S级跳过此阶段）
任务8：生成测试报告 docs/{需求标识}/05-test-report.md
          ↓ （流程闭环，必须更新知识库）
任务8.5：知识库增量更新 ← 不全量重扫，只更新本次变更涉及的章节
```

**需求变更流程：**
```
任务9.1 描述变更 → 9.2 AI变更影响分析 + 自动分级(S/M/L) → 9.3 用户确认范围和级别
          ↓
9.4 按对应级别流程执行：
  S级: 直接改码 → 简要报告 → 知识库更新
  M级: 增量重做受影响阶段 → 代码实现+测试 → 测试报告 → 知识库更新
  L级: 变更策略文档 → 按策略批量执行 → 回归验证 → 测试报告 → 知识库更新
```

- 如果做了任务2 → **所有后续阶段都会自动带上 `PROJECT_KNOWLEDGE_BASE.md` 让 AI 参考**，保证代码风格、目录结构和现有项目一致
- 如果跳过任务2（全新项目） → 从零开始，也完全正常

## 知识库贯穿整个开发流程（强制规则）

> 🎯 **核心思想：知识库 = 单一事实源（Single Source of Truth）**
> 所有开发阶段都必须「先读知识库，再写知识库」，不能脱离知识库单独进行。PUML 流程图是知识库的核心组成，也是设计和测试阶段的强制参考依据。

---

### 规则 1：PUML 流程图的标准格式与变更记录

**所有 PUML 文件必须包含：**
```plantuml
@startuml
' 项目：{项目名称}
' 流程图类型：{主业务流程/协议处理/OTA升级等}
' 生成时间：{YYYY-MM-DD}
' 激活标签：{对应特征标签}
'
' 变更记录（倒序排列，最新在最上面）：
' v{版本号} - {日期} - {变更内容描述}
' v{版本号} - {日期} - {初始版本}
'
skinparam backgroundColor #FEFEFE
skinparam rectangle {
  BackgroundColor #E8F4FD
  BorderColor #2196F3
}
skinparam note {
  BackgroundColor #FFF9C4
  BorderColor #FBC02D
}

title {流程图标题}

' 图内容...
@enduml
```

**强制要求：**
- 不能缺失任何元数据头部
- 版本号必须和 BASE.md 版本号一致
- 每次修改流程图必须追加变更记录
- 所有流程图必须使用统一的 skinparam 样式

---

### 规则 2：需求变更阶段必须先读知识库

- **必须做**：读取相关模块的 DETAIL 文档 + 对应的 PUML 流程图，评估变更影响范围
- **必须做**：变更完成后，**增量更新**受影响的知识库章节，并在变更记录中追加条目
- **禁止**：不看已有实现就凭空设计变更方案

---

### 规则 3：需求分析 / PRD 阶段必须建立知识库锚点

- **必须做**：读取 BASE.md 整体架构 + 技术栈约束，确保需求不超出已有技术能力
- **必须做**：参考同领域已有 PUML 流程图，复用已验证的实现模式
- **必须做**：在 PRD 中增加「知识库锚点」章节，标明：
  ```markdown
  ## 知识库锚点
  - 关联知识库版本：PROJECT_KNOWLEDGE_BASE.md @ v2.1
  - 关联模块域：加密模块域、升级模块域
  - 关联流程图：ota_flow.puml、security_flow.puml
  - 预计变更：将修改 ota_flow.puml 第 3-5 节点，新增降级流程
  ```
- **必须做**：新增需求涉及新流程的，在 PRD 中附带 PUML 流程图草稿

---

### 规则 4：软件设计阶段强制引用 PUML 流程图（必须校验）

✅ **必须引用已有 PUML 流程图作为基准**，不能脱离已实现的代码架构  
✅ 修改流程的，必须在设计文档中标注「此设计修改了 `{文件名}.puml` 中第 X 节点」  
✅ 新增流程的，设计评审通过后必须生成新的 `*.puml` 文件并入知识库  

❌ **禁止**：设计和已实现代码完全脱节，出现「设计一套、实现另一套」  
❌ **禁止**：只写设计文档不更新知识库，导致设计和实现两张皮  

---

### 规则 5：所有开发文档必须包含「知识库锚点」

需求分析文档、PRD、设计文档、测试报告，**每个文档顶部必须包含知识库锚点**，格式见规则 3。

---

### 规则 6：增量更新机制（禁止全量重写）

任何变更完成后，**禁止全量重写整个知识库**，必须按照"影响范围最小化"原则增量更新。

**三层同步更新矩阵**：

| 知识库文件 | 更新触发条件 | 更新方式 | 必须包含 |
|-----------|-----------|---------|---------|
| `PROJECT_KNOWLEDGE_BASE.md` (BASE) | 1. 新增模块<br>2. 架构重大调整<br>3. 技术栈变更 | 只更新版本号、变更记录索引、受影响的模块摘要 | ✅ 变更记录（倒序）<br>✅ 版本号递增 |
| `PROJECT_KNOWLEDGE_DETAIL-*.md` (DETAIL) | 1. 业务逻辑变更<br>2. 接口变更<br>3. 配置变更<br>4. 算法/流程变更 | 只修改受影响的模块域章节，其他保持不动 | ✅ 变更记录（倒序）<br>✅ 变更影响范围说明 |
| `docs/knowledge-base/*.puml` (流程图) | ⚠️ **只要代码流程变了就必须更新**<br>1. 状态机状态/跃迁变更<br>2. 调用链变更<br>3. 业务流程变更<br>4. 模块依赖关系变更<br>5. 协议报文格式变更 | ✅ **绝对禁止删掉重写**<br>只修改图中有变化的节点/箭头<br>不影响的部分保持原样 | ✅ 变更记录追加一行（倒序）<br>✅ 版本号和 BASE 同步 |

**强制执行标准**：
- 代码合并前必须先完成对应的知识库更新
- PUML 更新和代码更新必须在同一个 MR/PR 中提交
- 只要代码中流程逻辑变了，对应的 PUML 必须同步更新
- 设计文档中引用的 PUML 节点编号变更时必须同步更新
- 所有文件的版本号必须保持一致（BASE v2.3 → 所有 PUML 也是 v2.3）

---

### 规则 7：设计评审必须包含「知识库一致性检查」

设计评审的 checklist 必须增加：
- [ ] 设计是否引用了对应的知识库 PUML 流程图？
- [ ] 预计的知识库变更范围是否明确？
- [ ] 设计和已有实现是否存在冲突？
- [ ] 变更完成后谁负责更新知识库？

---

### 规则 6.1：测试报告完成后的知识库更新 SOP

测试报告确认通过后，**48 小时内必须完成对应的知识库增量更新**，按以下顺序执行：

**Step 1：更新 PUML 流程图（优先级最高）**
1. 根据本次变更的影响范围，找出需要更新的 .puml 文件
2. **只修改有变化的节点/箭头**，不影响的部分保持不动
3. 在文件头部「变更记录」中追加一行新记录（版本号与 BASE 同步）
4. 在线渲染验证无误后提交

**Step 2：更新 DETAIL 文档**
1. 只修改受影响模块域对应的章节
2. 在章节顶部追加「变更记录」说明
3. 同步更新接口、配置、数据结构等有变化的内容

**Step 3：更新 BASE 文档**
1. 版本号 + 1（如 v2.3 → v2.4）
2. 在「变更历史」章节追加本次变更记录
3. 更新受影响的模块摘要
4. 更新「PUML 流程图索引」表格中的版本信息

**Step 4：一致性校验**
- [ ] BASE、DETAIL、PUML 三者版本号完全一致
- [ ] 所有变更的 PUML 都追加了变更记录
- [ ] 代码中提到的流程编号和 PUML 中的节点可以对应
- [ ] 在线渲染所有更新的 PUML，确认无语法错误

---

### 知识库分层总结

| 文件 | 定位 | 更新频率 | 优先级 |
|------|------|---------|-------|
| `docs/knowledge-base/*.puml` | 流程事实标准 | ⚠️ **实时**，代码流程变了必须先更图 | P0 最高 |
| `PROJECT_KNOWLEDGE_DETAIL-*.md` | 深度备查手册 | 中频，每次需求涉及核心模块时更新 | P1 |
| `PROJECT_KNOWLEDGE_BASE.md` (BASE) | 快速入门索引 | 低频，只在重大架构变更时更新 | P2 |

---

## 常见问题

### Q1: 如果我随便开一个 Claude Code 会话就可以使用吗？

**A:** 必须 `cd` 到 autopilot 目录启动。Skills 虽然全局可用，但流程规则（CLAUDE.md、行为约束）只在项目目录下加载。

```bash
cd L:/jit/claude-code-autopilot
claude
```

启动后看到 `✅ Autopilot 智能流水线 v{版本号} — 就绪` 表示加载成功。

---

### Q2: 我可以在其他项目使用这个流程吗？

**A:** 可以。设计上就是**共用框架 + 多项目分离产出**，支持同时处理多个项目。

**使用方式：**
1. 始终在 autopilot 目录启动 Claude Code
2. 处理哪个项目，就提供那个项目的路径
3. 所有文档都输出到对应项目自己的目录下
4. 框架目录保持干净，不会和项目文件混在一起，避免冲突

---

### Q3: 是否必须从需求分析开始？可以直接从 PRD 开始吗？

**A:** 流程是按顺序设计的，但你可以跳步。如果你确认需求已经非常清晰，可以在一开始就说：
> "需求已经澄清，直接生成 PRD，跳过需求分析的待澄清环节"

AI 会直接生成 PRD。推荐按流程走一遍，需求分析阶段能帮你发现很多考虑不周的地方。

---

### Q4: 每个阶段都必须人工确认吗？可以全自动一次性跑完吗？

**A:** 本流程设计就是**每个阶段需要人工确认**，原因：
- AI 理解需求可能出错，早发现早修复成本更低
- 你对项目的需求和设计有控制权，可以随时调整方向

如果你对 AI 很有信心，可以说：
> "一次性生成需求分析、PRD、设计文档，然后我一起评审"

AI 会连续生成多个文档，然后一起给你评审。

---

### Q5: 如果评审发现问题怎么办？

**A:** 很简单，直接告诉 AI 哪里需要改，改什么：
> "PRD 里第三个功能需求不对，应该改成 xxx"

AI 会修改对应文档，修改完你再重新确认。

---

### Q6: 历史项目私域知识库一定要生成吗？

**A:**
- ✅ **需要做：** 在现有历史项目上开发新功能 → 帮 AI 理解现有代码
- ❌ **可以跳过：** 从全新空白开始新项目 → 不需要历史项目，直接跳过

---

### Q7: 这个流程和 Git 的关系是什么？每个任务都要提交吗？

**A:** 是的，每个任务完成后都会自动 Git 提交（提交在目标项目，不是框架目录）。好处：
- 每个阶段的产出都有版本记录
- 如果需要回滚，可以回到上一个阶段
- 你可以清楚看到每个阶段做了什么

---

### Q8: 如果我不想要某个阶段的产出，可以删掉重来吗？

**A:** 完全可以。Git 可以很方便地回滚：
```bash
# 在目标项目目录
cd 目标项目路径
git log                   # 查看提交历史
git reset --hard <commit-id>   # 回滚到指定提交
```
然后重新开始就行。

---

### Q9: 如果我忘了用 `/project-autopilot-status`，是否每次都需要带目录？

**A:**
- ✅ **只要你第一次提供目标项目路径**，我就会记住，后续阶段**不需要**每次都带
- ✅ `/project-autopilot-status` 主要用于：
  1. 新开 Claude 会话后，恢复记忆告诉你当前在哪个项目
  2. 切换到另一个项目开发
  3. 忘记当前项目路径时，查看状态
- 如果你一直在同一个会话中处理同一个项目，完全不需要反复用这个命令

---

### Q10: 需求变了，要重新从头走一遍流程吗？

**A:** ❌ 不需要。使用任务9（需求变更），AI 会自动分析变更影响范围，只增量重做受影响的阶段。比如只是改个按钮文案，可能只需要更新 PRD 和代码，不需要重新做架构设计。

---

### Q11: 知识库每次都要全量重新生成吗？

**A:** ❌ 不需要。
- **首次**（任务2）是全量扫描，理解整个项目
- **后续更新**（任务8.5）都是增量，只更新本次变更涉及的章节
- 不管是正常需求实现还是需求变更，流程走完后都会自动执行知识库增量更新
- 知识库末尾有更新记录表，可以清楚看到每次更新了什么

---

### Q12: 任务9需求变更时，我需要判断变更大小吗？

**A:** ❌ 不需要。你只需要描述变更内容，AI 会自动分析影响范围并告诉你需要重做哪些阶段。你确认范围后 AI 才开始执行。

---

## 原理图

```
=== 场景1：正常需求实现 ===
你的简单需求 + 目标项目路径
    ↓
需求分析 → [你确认澄清]
    ↓
PRD → [你确认]
    ↓
软件设计 → [你确认]
    ↓
代码实现 (TDD) → [你确认]
    ↓
测试用例 → [你确认]
    ↓
测试报告 → [你确认]
    ↓
知识库增量更新 → 完成

=== 场景2：需求变更（自动分级） ===
你的变更描述
    ↓
AI 变更影响分析 + 自动分级 → [你确认范围和级别]
    ↓
┌──────────┬──────────────────┬──────────────────────┐
│ S 级     │ M 级             │ L 级                 │
│ 小变更   │ 中变更           │ 大变更               │
├──────────┼──────────────────┼──────────────────────┤
│ 直接改码 │ 增量重做受影响   │ 变更策略文档         │
│ ↓        │ 阶段(标准流程)   │ ↓                    │
│ 简要报告 │ ↓                │ 按策略批量执行       │
│ ↓        │ 代码实现+测试    │ ↓                    │
│ 知识库   │ ↓                │ 回归验证             │
│ 更新     │ 测试报告         │ ↓                    │
│          │ ↓                │ 测试报告             │
│          │ 知识库更新       │ ↓                    │
│          │                  │ 知识库更新           │
└──────────┴──────────────────┴──────────────────────┘
```

所有产出都保存在**目标项目目录**，框架目录保持干净。每个箭头处都有你的人工确认，保证方向不跑偏。**不管哪种场景，流程最后都必须更新知识库，保证知识库始终和代码同步。**

---

---

## 许可证

本流程基于 [claude-code-collective](https://github.com/claude-code-collective/claude-code-collective) 开源社区组件构建，遵循原项目许可证。
