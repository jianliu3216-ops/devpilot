#!/bin/bash
# DEPRECATED (v1.5): Routing is now handled by DECISION.md auto-delegation system.
# This hook is no longer registered in settings.json and will be removed in v2.0.
# Replacement: .claude-collective/DECISION.md + test-driven-handoff.sh handoff detection
exit 0