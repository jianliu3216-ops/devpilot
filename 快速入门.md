# Autopilot 智能流水线 — 快速入门

## 30 秒看懂

Autopilot 让你**用一句话驱动 AI 完成整个开发流程**：需求分析 → PRD → 设计 → 编码 → 测试 → 报告。

```
# 在项目目录下启动 Claude Code，激活后直接说话
你：需求分析：我要做用户登录模块
AI：自动评估级别、生成需求文档、等你确认后继续
```

---

## 前置要求

| 依赖 | 版本 | 检查 |
|------|------|------|
| Node.js | >= 18 | `node -v` |
| git | 任意 | `git --version` |
| Claude Code | 最新 | `npm install -g @anthropic-ai/claude-code` |
| CodeGraph（可选） | Node >= 22.12.0 | `npm install -g @optave/codegraph`（install.sh 不自动装，自行安装见 [docs/CodeGraph 安装指南.md](docs/CodeGraph 安装指南.md)） |

> CodeGraph 为可选加速工具：预生成依赖图，减少知识库扫描的上下文消耗 70-80%，变更影响分析更精确。大项目强烈推荐，小项目可跳过。未安装时知识库 Skill 自动回退为全量扫描，不影响流程。

## 安装

```bash
git clone <仓库地址> claude-code-autopilot
cd claude-code-autopilot
bash install.sh --tools    # Windows 请用 Git Bash 执行
```

**Windows 说明**：安装脚本 `install.sh`、`update-skills.sh`、`node-detect.sh` 需 **Git Bash 或 WSL**。

框架升级后补装新 Skill：`bash update-skills.sh`

## 启动

```bash
# 在项目目录下启动 Claude Code
cd D:\my-project
claude

# 首次使用：激活流水线（显示操作菜单）
/jit-devpilot-init

# 之后每次新开会话：一键恢复（自动激活 + 显示进度）
/jit-project-autopilot-status
```

> `/jit-project-autopilot-status` 会自动检测当前目录为目标项目，无需再次 init。切换项目时用 `/jit-devpilot-init`。

---

## 怎么用

**你只需要用自然语言说话**，AI 自动识别进入对应阶段。**目标项目路径只需指定一次**，后续自动继承。

| 你想做什么 | 这样说 |
|-----------|--------|
| 让 AI 理解现有项目 | `生成知识库`（首次需指定目标项目） |
| 开始一个新需求 | `需求分析：功能描述` |
| 生成 PRD | `生成PRD` |
| 软件设计 | `软件设计` 或 `变更策略` |
| 写代码 | `代码实现` 或 `开始编码` |
| 生成测试用例 | `测试用例` 或 `回归验证` |
| 跑测试出报告 | `测试报告` 或 `运行测试` |
| 修改已有需求 | `需求变更：user-login 增加忘记密码` |
| 更新知识库 | `知识库更新` |
| 查看项目进度 | `/jit-project-autopilot-status` |

> 每个阶段完成后 AI 会等你确认，你可以随时说"一次性生成全部文档，我一起评审"来跳过确认。

可用的 Skill 命令：`/jit-devpilot-init` `/jit-project-knowledge-base` `/jit-project-autopilot-status` `/jit-env-auto-setup` `/jit-ui-ux-pro-max`（Tab 可自动补全）

---

## 一个完整例子

```
👤 /jit-devpilot-init              # 在项目目录下首次激活
🤖 检测到当前目录 D:\my-app，是否作为目标项目？
👤 是

👤 需求分析：用户登录模块，支持手机号+密码登录
🤖 需求标识建议：user-login → [创建 00-原始需求.md]
🤖 📊 M 级（约8文件，2模块）。确认？

👤 确认  →  🤖 需求分析完成
👤 生成PRD →  🤖 PRD 完成
👤 软件设计 →  🤖 设计完成
👤 开始编码 →  🤖 代码+测试完成
👤 测试报告 →  🤖 12/12 通过 → 知识库已更新 🎉
```

---

## 项目知识库（重要）

**历史项目接入时，先说一句**：

```
生成知识库
```

AI 自动扫描项目，生成架构总览 + 模块字典 + 编码规范。后续所有阶段自动参考，**一次生成，全流程复用**。代码变更后说 `知识库更新` 增量同步。

---

## 变更级别

AI 会自动评估每个需求的规模，按级别调整流程：

| 级别 | 文件数 | 流程 |
|------|:---:|------|
| **S** | ≤3 | 简要分析 → 直接改 → 简要报告 |
| **M** | 4-15 | 完整 6 阶段：分析 → PRD → 设计 → TDD编码 → 测试用例 → 测试报告 |
| **L** | >15 | 策略文档 → 分批执行 → 回归验证 |

不指定级别时 AI 自动评估并请你确认。你也可以直接说 `级别：M`。

---

## 目录结构（按需求隔离）

```
目标项目/
├── docs/
│   ├── knowledge-base/           # 项目知识库（全局共享）
│   ├── user-login/               # 需求A（独立目录）
│   │   ├── 00-原始需求.md
│   │   ├── 01-requirements-analysis.md
│   │   ├── 02-prd.md
│   │   ├── 03-software-design.md
│   │   ├── 04-test-cases.md
│   │   ├── 05-test-report.md
│   │   └── CHANGELOG.md
│   └── another-feature/          # 需求B（互不干扰）
└── tests/{需求标识}/             # 测试代码
```

---

## 常见问题

**Q: 必须按顺序走完所有阶段吗？**
A: 不一定。你可以说"跳过设计，直接编码"或"一次性生成分析和PRD，我一起看"。

**Q: 中途需求变了怎么办？**
A: 说 `需求变更：xxx`，AI 自动分析影响范围，只重做受影响的部分，不用从头来。

**Q: 新开会话怎么恢复？**
A: `/jit-project-autopilot-status 目标项目路径`，AI 自动扫描已有产出，告诉你进度。

**Q: 知识库每次都要重建吗？**
A: 不用。首次全量扫描，后续 `知识库更新` 只增量更新变更部分。

---

> 📖 完整流程规则详见 [README.md](README.md) | 升级指南 [升级指南.md](升级指南.md) | 定制拓展 [docs/定制与拓展.md](docs/定制与拓展.md)
